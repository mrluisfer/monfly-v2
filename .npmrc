engine-strict=true
# Solo resuelve versiones publicadas hace 7+ dias (en minutos): da margen a que
# se detecte un paquete comprometido. No afecta a --frozen-lockfile, y Dependabot
# lo omite en security updates.
minimum-release-age=10080
