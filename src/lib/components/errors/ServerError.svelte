<!--
	500, and for now every status that isn't 404: something threw — a load that
	failed, the database out of reach, a bug. Inside the app it sits under the
	header, so the tabs are still there to leave by; when the app's own layout is
	what failed, it fills the window instead. The cause is never shown here:
	SvelteKit hands the page "Internal Error", and the exception goes to the
	server's log.

	Empty on purpose: this is where its picture goes.
-->
<div class="flex-1"></div>
