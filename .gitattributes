# LF in the repo and on checkout, on every OS.
* text=auto eol=lf

# Binary assets: never diff or normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.avif binary
*.ico binary
*.woff binary
*.woff2 binary

# Collapse the lockfile in GitHub diffs.
pnpm-lock.yaml linguist-generated=true
