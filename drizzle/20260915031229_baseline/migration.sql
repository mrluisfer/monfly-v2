-- Baseline: the database as it stood when Drizzle Kit took over its migrations from
-- monfly-v1's Prisma. Its structure is snapshot.json, which the next migration is
-- diffed against; there is nothing to run, so recording it is all migrate does.
SELECT 1;
